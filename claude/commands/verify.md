# 코드 검증

작업 완료 후 코드를 검증합니다.

## 검증 순서
1. TypeScript 타입 체크 (있는 경우)
2. 린트 실행
3. 테스트 실행
4. 빌드 실행

## 실행할 명령어
```bash
# package.json 확인
cat package.json | grep -A 20 '"scripts"'
```

각 단계에서 에러가 발생하면 즉시 수정하고 다시 검증합니다.
검증이 모두 통과할 때까지 반복합니다.
