name: app_kit
description: App shell — go_router StatefulShell + ChromeController over core/ds.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: '^3.6.0'
  flutter: '>=3.24.0'

dependencies:
  app_links: ^7.1.1
  core:
    path: ../core
  crypto: ^3.0.6
  dio: ^5.9.0
  ds:
    path: ../ds
  firebase_analytics: ^12.0.0
  firebase_core: ^4.9.0
  firebase_messaging: ^16.2.2
  flutter:
    sdk: flutter
  flutter_local_notifications: ^21.0.0
  flutter_riverpod: ^3.0.0
  flutter_secure_storage: ^9.2.4
  go_router: ^17.2.3
  gotrue: ^2.20.0
  kakao_flutter_sdk_user: ^1.9.7
  local_auth: ^2.3.0
  meta: ^1.15.0
  shared_preferences: ^2.5.5
  sign_in_with_apple: ^7.0.1
  supabase_flutter: ^2.12.0
  tosspayments_widget_sdk_flutter: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true
